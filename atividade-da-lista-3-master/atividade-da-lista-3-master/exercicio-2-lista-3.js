// exercicio 2

function calculator (compradas, vendidas) {
    let a = compradas = compradas * 25;
    let b = vendidas = vendidas * 40;

    let calculator = b - a;

    return "O lucro é de R$" + calculator;

}
console.log(calculator(1,2));